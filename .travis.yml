dist: bionic
language: python
cache: pip
python:
  - "3.6"

install:
  - pip install -U pip
  - pip install -r devel.txt

script:
  - make check

after_success:
  - coveralls

notifications:
  email:
    on_failure: change
    on_success: never
